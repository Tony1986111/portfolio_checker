[build]
builder = "nixpacks"

[deploy]
startCommand = "./target/release/portfolio_backend"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
